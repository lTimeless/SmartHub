mutation Registration($input: RegistrationInput!) {
  registration(input: $input) {
    isAuthenticated
    user {
      id
      userName
      roles
    }
    errors {
      message
      code
    }
    message
  }
}