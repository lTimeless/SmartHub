mutation Login($input: LoginInput!) {
  login(input: $input) {
    isAuthenticated
    user {
      id
      userName
      isFirstLogin
      roles
    }
    errors {
      message
      code
    }
    message
  }
}